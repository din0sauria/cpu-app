# 页面内容设计清单

> 本文档详细列出 CPU 漏洞检测平台（NeuralCore）各页面的内容元素，基于 2026-04 最新代码状态编写。

---

## 一、项目概述

| 维度 | 说明 |
|------|------|
| 平台名称 | NeuralCore — AI CPU 漏洞检测平台 |
| 前端框架 | Vue 3 (Composition API) + Vite + Pinia + Vue Router |
| 可视化库 | ECharts + echarts-gl + @jiaminghi/data-view |
| 其他依赖 | markdown-it（报告渲染）、@vueuse/core、three（预留） |
| 设计风格 | 暗色科技风 / 毛玻璃卡片 / 霓虹渐变 |
| 构建模式 | SPA（Hash History），支持单文件构建 |

---

## 二、全局通用元素

### 2.1 顶部导航栏 (AppLayout)
- **平台 Logo** — "NeuralCore" 渐变文字 + 副标题 "AI CPU漏洞检测平台"
- **导航菜单** — 首页 / 漏洞详情 / 主机漏洞检测 / 代码检测
- **系统信息选择器** — 顶部右侧显示当前选择的 CPU 型号和操作系统，点击弹出弹窗可切换
- **移动端适配** — 汉堡菜单 + 侧边栏抽屉 + 遮罩层

### 2.2 左侧边栏 (AppLayout)
- **Logo 区域** — 平台名称与副标题
- **导航链接** — RouterLink 样式，当前页高亮（渐变背景 + 发光阴影）
- **底部系统按钮** — 显示当前 CPU 型号 / OS，点击打开系统信息选择弹窗

### 2.3 弹窗组件
- **系统信息选择弹窗** — CPU 型号下拉（12 种）+ 操作系统下拉（9 种）+ 保存按钮
- **漏洞详情弹窗** — 基本信息网格 + 适用平台 + 操作按钮（查看实验报告 / 下载 POC / 下载 EXP）
- **实验报告弹窗** — 加载 Markdown 报告并渲染为 HTML，支持滚动

---

## 三、数据大屏页面 (Dashboard.vue)

### 3.1 平台概览卡片
- **下载请求数** — 动态数值 + 周增长百分比
- **演示请求数** — 动态数值 + 增长百分比
- **POC 总数** — 从 vulnStore 统计 + 本周新增数量
- **EXP 演示数量** — 从 vulnStore 统计 + 增长百分比
- **AI 漏洞代码识别率** — 百分比数值 + 变化量

### 3.2 CVE 类型分布饼图
- 数据来源：`vulnStore.vulnerabilities` 按 `cveType` 聚合
- 环形图（40%-70% 半径），右侧纵向图例
- 颜色方案：`#00d4ff, #00ff9d, #ff3366, #ffaa00`

### 3.3 攻击类型统计条形图
- 数据来源：按 `attackType` 聚合后按数量降序
- 横向条形图，渐变色填充（科技蓝 → 荧光绿）
- 右侧显示具体数值标签

### 3.4 漏洞分布热力图
- X 轴：Windows / Linux / macOS / 其他
- Y 轴：Intel / AMD / Apple M / 其他
- 颜色范围：透明 → 绿 → 黄 → 红

### 3.5 漏洞资源增长趋势图
- 双折线（POC 数量 / EXP 数量）+ 面积渐变填充
- X 轴：周一至周日，Y 轴：数量

### 3.6 漏洞资源知识库
- 展示前 12 个漏洞卡片网格
- 每个卡片：漏洞名称、攻击类型、风险等级标签
- 左侧边框颜色区分风险等级（红 / 黄 / 绿）
- 6 列响应式布局，大屏 6 列 → 中屏 4 列 → 平板 3 列

### 3.7 AI 漏洞分析流程示意
- **代码扫描窗口** — 模拟展示 `vulnerable_code.c` 的扫描过程
  - 行号 + 代码文本 + 漏洞标签
  - 逐行扫描动画（1.5s 切换），扫描中行为黄色高亮
- **流程步骤** — 📥 上传代码 → 🔍 静态分析 → 🎯 漏洞匹配 → ⏱️ 风险评估 → 📄 报告生成
  - 步骤每 2 秒依次高亮（绿色发光边框）

### 3.8 布局网格
- 12 列 CSS Grid 布局
- 响应式断点：≥1400px 标准 / 1024-1399px 双列合并 / <1024px 单列

---

## 四、漏洞详情页面 (POCView.vue)

### 4.1 搜索筛选区域
- **搜索框** — 实时过滤，匹配 name / summary / cveType / cveId / attackType
- **CVE 类型下拉** — 从 vulnStore 动态生成，首选项 "全部类型"
- **攻击类型下拉** — 从 vulnStore 动态生成，首选项 "全部攻击类型"
- **处理器架构下拉** — 从 vulnStore 动态生成，首选项 "全部架构"
- **风险等级下拉** — 全部 / 高危 / 中危 / 低危
- **结果计数** — 实时显示 "找到 N 个漏洞详情"

### 4.2 漏洞卡片网格
- 3 列网格布局（≤1400px → 2 列 → ≤900px → 1 列）
- **卡片头部** — 漏洞名称（Orbitron 字体）+ 风险等级徽章
- **卡片元信息** — CVE 编号 / 攻击类型 / 处理器架构
- **描述文字** — 最多 3 行省略
- **代码类型标签** — POC 标签（蓝色）/ EXP 标签（红色）
- **统计信息** — 平均检测用时
- **操作按钮** — 查看详情 / 下载 POC / 下载 EXP

### 4.3 详情弹窗
- 基本信息网格（2 列）：漏洞名 / CVE 编号 / 攻击类型 / 风险级别 / 处理器平台 / 平均检测用时
- 一句话简介
- 适用平台标签列表
- 操作按钮：🧪 查看实验报告 / ⬇️ 下载 POC 代码 / ⬇️ 下载 EXP 代码

### 4.4 实验报告弹窗
- 弹窗标题：`{漏洞名} 实验报告`
- 报告路径：优先使用 `vuln.reportPath`，回退到 `/reports/{漏洞名}.md`
- 使用 `markdown-it` 将 Markdown 渲染为 HTML
- 样式适配暗色主题

### 4.5 下载功能
- 调用后端 API：`GET /api/v1/vulnerabilities/by-name/{name}/artifacts/{poc|exp}`
- 从 `Content-Disposition` 解析文件名，回退使用默认命名
- Blob 方式创建临时下载链接

---

## 五、主机漏洞检测页面 (DemoView.vue)

### 5.1 模块选择与命令生成
- **可用模块列表** — 从 `/api/v1/modules` 获取，分页显示（每页 8 个）
- **多选复选框** — 选择要检测的漏洞模块
- **生成命令按钮** — 调用 `/api/v1/bundles/create` 创建检测任务
- **命令展示** — 显示平台类型、预计运行时间、完整命令行
- **复制命令 / 使用此命令会话**

### 5.2 连接与控制
- **状态面板** — 会话 ID / 连接状态 / Agent 保活状态（带指示灯动画）
- **控制按钮** — 开始检测 / 停止检测 / 暂停 / 继续 / 跳过当前 / 终止连接
- **WebSocket 连接** — 连接前端 WS 地址接收实时事件

### 5.3 当前检测窗口
- **检测信息胶囊** — 当前模块 / 阶段 / 预计时间 / 已耗时 / 剩余估计
- **运行摘要** — 实时文字描述
- **关键事件面板** — 最近 12 条关键事件，按状态着色（成功/警告/错误/信息）
- **阶段进度条** — 阶段节点序列 + 进度百分比条 + 阶段描述文字
- **实时日志** — 时间戳 + 内容，按级别着色，200px 高度滚动

### 5.4 检测结果列表
- 模块名 / 开始时间 / 结束时间 / 结论（存在漏洞 / 未发现 / 未知）
- 查看报告按钮

### 5.5 报告缓存（按检测批次）
- 批次按时间排列，支持展开 / 收起
- 删除批次功能
- 数据持久化到 `localStorage`

---

## 六、代码检测页面 (DetectView.vue)

### 6.1 任务配置面板
- **ZIP 文件上传区** — 支持拖拽上传和点击选择，显示文件名和大小，可移除
- **target_bin 输入框** — 可选参数，指定目标二进制名
- **detectors 多选框** — flush_reload_branch / prime_probe_array
- **操作按钮** — 上传并分析（主按钮）/ 断开 SSE（次按钮）
- **元信息** — task_id 标签 + status 标签（带颜色状态）

### 6.2 流程阶段面板
- 5 个阶段：Phase 0 初始化 / Phase 1 预处理 / Phase 2 静态分析 / Phase 3 AI 审计 / Phase 4 报告生成
- 阶段指示器（底部进度线），状态：pending / active / completed
- 阶段描述文字

### 6.3 实时控制台
- 日志输出区（最大 280px 高度），自动滚动
- 日志行格式：`[时间] [类型] 消息内容`
- 日志类型着色：info(蓝) / warning(橙) / error(红) / AI(浅蓝) / control(绿)
- 最多 500 条日志

### 6.4 Agent 工具查询可视化
- **脉冲动画** — 三层脉冲环 + 中心圆点，工具查询时心跳动画
- **状态标签** — 等待 / 工具查询中 / 查询完成
- **批次进度条** — 显示当前处理批次 / 总批次 / 百分比
- **最近一次工具请求** — 显示查询摘要
- **最近一次工具结果摘要** — 显示结果摘要

### 6.5 数据表格面板
- **candidates 表格** — name / detector / pattern / imbalance / args
- **shortlist 列表** — 有序列表展示
- **audit_reports 表格** — func_name / attack_type / leaked_info / reason，点击行生成 EXP
- **final_report** — 最终报告文本展示

### 6.6 EXP 生成面板
- 点击 audit_reports 表格行后自动生成攻击代码模板
- 代码展示区（300px 高度滚动）
- 复制代码 / 下载代码按钮

### 6.7 SSE 实时通信
- 连接 `POST /upload` 创建任务 → `GET /stream/{task_id}` 接收 SSE 事件
- 事件类型：start / info / warning / error / phase_0~4 / candidates / shortlist / audit_reports / final_report / agent_step / tool_call / tool_result / finished / complete

---

## 七、漏洞详情页面 (VulnDetail.vue)

### 7.1 基本信息区
- 漏洞名称（大字标题）
- 风险等级徽章 + CVE 类型标签 + 架构标签
- 下载按钮：下载 POC / 下载 EXP

### 7.2 标签页切换
- **📌 基本信息** — 攻击原理描述 / 统计信息（成功率 / 平均耗时）/ 支持平台标签 / CPU 型号标签 / 漏洞标签
- **💻 代码示例** — POC 代码 / 攻击者代码 / 受害者代码，分别可下载
- **📄 分析报告** — 占位提示（对应 MD 报告模板位置）

---

## 八、响应式断点

| 断点 | 宽度 | 主要调整 |
|------|------|---------|
| 大屏 | ≥1400px | 标准 12 列网格 |
| 桌面 | 1200-1399px | 图表双列合并 |
| 平板 | 992-1199px | 漏洞卡片 2 列 |
| 手机 | <768px | 单列 / 汉堡菜单 |

---

## 九、颜色规范

### 9.1 主色调（CSS 变量）
```
--primary:     #1a3d7c    深蓝
--secondary:   #00d4ff    科技蓝
--accent:      #00ff9d    荧光绿
--danger:      #ff3366    警示红
--warning:     #ffaa00    橙色
--info:        #00d4ff    信息蓝
--success:     #00ff9d    成功绿
--bg-dark:     #0a0e27    背景深
--bg-card:     rgba(16,22,58,0.8)    卡片背景
--border-glow: rgba(0,212,255,0.3)   发光边框
```

### 9.2 风险等级
- 高危：`#ff3366` 红 / 中危：`#ffaa00` 黄 / 低危：`#00ff9d` 绿

### 9.3 文字颜色
- 主标题：`#ffffff` / 正文：`rgba(255,255,255,0.85)` / 次要：`rgba(255,255,255,0.6)`

---

## 十、字体规范

| 用途 | 字体 |
|------|------|
| 标题 / 数字 | Orbitron (Google Fonts) |
| 正文 | Rajdhani (Google Fonts) |
| 代码 | IBM Plex Mono / Courier New |

---

## 十一、动画效果汇总

| 元素 | 动画类型 | 时长 |
|------|---------|------|
| 数字变化 | 自动计数更新 | 实时 |
| 流程步骤 | 依次高亮 | 2s 切换 |
| 代码扫描行 | 逐行高亮 | 1.5s 切换 |
| 卡片悬停 | 上浮 + 发光 | 0.3s |
| 面板入场 | scaleIn + fadeIn | 0.5s 延迟递增 |
| 脉冲环 | expand + fade | 2s 循环 |
| 日志条目 | slideIn | 0.25s |
| 背景网格 | perspective 移动 | 20s 循环 |
| 心跳指示灯 | blink | 1.1s 循环 |
