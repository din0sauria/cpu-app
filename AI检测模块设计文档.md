# AI侧信道漏洞检测模块设计文档

## 模块概述

AI侧信道漏洞检测模块是侧信道漏洞自动化发现与AI审计系统的核心组件，采用混合分析架构，将传统的LLVM IR静态指令流图分析与现代代码属性图（CPG）技术、以及具备自主工具调用能力的AI Agent相结合。该模块旨在从C/C++等系统级源码中，自动捕获由于数据依赖、执行时间失衡、存储或分支逻辑设计不当引入的各类侧信道攻击受害面，并通过全自动化工作流输出高准确率的深度审计报告。

## 核心检测思路

### 混合分析架构

检测模块采用四阶段流水线架构，实现从源码到漏洞报告的端到端自动化分析。第一阶段为预处理与编译提取，系统接收C/C++项目源码后，通过拦截编译系统注入特定编译标志，提取整合后的LLVM Bitcode中间层表示文件，并调用Joern解析源码生成代码属性图数据库。第二阶段为静态执行流多模式筛查，引擎遍历每条函数的基本块和底层指令，识别侧信道薄弱点规则，提炼具有侧信道泄露潜质的候选函数池。第三阶段为AI业务语义降噪与打标，结合项目上下文指纹与静态提取的高危函数调用链，快速过滤不涉及核心资产的浅层风险函数。第四阶段为多轮Agent溯源推理与定等，基于工具链驱动的大语言模型审查链路进行深度分析。

### 漏洞检测模式

检测模块目前支持四种侧信道漏洞检测模式。Flush+Reload分支检测模式通过分析控制流分支两侧基本块的开销权重失衡，识别基于数据依赖分支的缓存侧信道泄露点。Prime+Probe数组检测模式通过追踪数据依赖的数组索引访问，识别基于秘密相关内存访问模式的缓存竞争泄露点。Spectre V2检测模式通过识别间接分支、间接调用、内联汇编跳转等危险指令模式，检测分支目标注入漏洞的潜在受害点。Spectre V4检测模式通过污点传播引擎追踪Store Buffer中的陈旧数据流向，检测推测存储绕过漏洞的潜在泄露点。

## 算法实现方法

### 静态分析引擎

静态分析引擎基于llvmlite库实现LLVM Bitcode文件的解析与遍历。引擎首先加载.bc文件并解析为LLVM模块对象，然后遍历模块中的所有函数定义。对于每个非声明函数，引擎提取其参数列表、返回类型、基本块结构等元信息，并构建指令映射表用于后续的数据依赖分析。

**基本块开销计算算法**：引擎为每条LLVM IR指令赋予不同的开销权重，其中call指令权重为50，mul指令权重为10，shl指令权重为5，其他指令按行数计算基础开销。通过累加基本块内所有指令的权重，得到该基本块的执行时间估计值。对于条件分支指令，计算两个目标基本块的开销差值，当差值超过阈值（默认40）且分支条件存在数据依赖时，标记为潜在的Flush+Reload泄露点。

**数据依赖追踪算法**：引擎采用递归遍历方式追踪值的数据依赖链。对于load指令和phi指令，直接标记为数据依赖源。对于其他指令，递归检查其操作数是否存在数据依赖。该算法通过visited集合避免循环依赖导致的无限递归，确保分析的可终止性。

**污点传播引擎**：针对Spectre V4检测，引擎实现了完整的污点传播机制。首先监控Store指令记录写入位置，然后在推测窗口范围内（默认30条指令）监控Load指令作为污点源。对于后续的算术运算、位运算、类型转换等指令，如果其操作数包含污点寄存器，则将结果寄存器也标记为污点，并继承原始污点的行号信息。当污点数据流经getelementptr或load指令时，触发泄露点告警。

### 间接分支检测算法

Spectre V2检测器实现了多层次的间接分支识别机制。第一层检测原生计算型间接分支，通过识别indirectbr指令直接定位危险点。第二层检测内联汇编中的间接调用，通过正则表达式匹配call *%rax或jmp *%rdx等模式，识别隐藏在汇编代码中的间接跳转。第三层检测标准的C/C++间接调用，通过正则表达式匹配call指令后跟寄存器操作数的模式，识别函数指针调用和C++虚函数分发。检测器同时调用c++filt工具对C++修饰名进行还原，确保输出结果的可读性。

### AI推理引擎

AI推理引擎基于OpenAI兼容接口调用DeepSeek大模型，实现了具备工具调用能力的Agent架构。引擎通过Function Calling机制定义lookup_symbol工具，允许AI在分析过程中主动查询未知的结构体定义、宏定义或类型声明。引擎维护完整的对话上下文，支持多轮交互式分析，确保AI能够逐步深入理解代码语义。

**攻击类型规范化算法**：引擎实现了严格的攻击类型校验机制。首先对原始攻击类型标签进行归一化处理（去除空格、连字符、大小写统一），然后在预定义的攻击类型白名单中进行匹配。对于不在白名单内的攻击类型，强制转换为"无"，并在reason字段中说明原因。该机制有效约束了AI的输出范围，避免了幻觉问题导致的误报。

**函数级模板约束**：引擎为每个候选函数维护独立的攻击类型约束列表，该列表来源于静态检测阶段命中的漏洞模板。AI在输出结论时必须遵守函数级约束，即只能在该函数命中的模板范围内给出attack_type。如果AI推理出其他类型，必须输出"无"。该机制确保了静态分析与AI推理的一致性。

## 数据处理流程

### 输入接口定义

模块支持两种输入方式。第一种为项目压缩包上传，前端通过multipart/form-data协议上传ZIP格式的项目源码包，可选择性指定目标二进制文件名和检测器列表。第二种为本地路径输入，直接指定已解压的项目目录路径，适用于命令行调用场景。

输入参数包括：project_dir（项目源码目录，必填）、output_dir（中间文件输出目录，必填）、target_bin（目标二进制文件名，可选）、detectors（检测器列表，可选，默认为全部检测器）。其中detectors参数支持flush_reload_branch、prime_probe_array、spectre_v2、spectre_v4四种模式，多个检测器以英文逗号分隔。

### 中间数据流转

预处理阶段生成三类中间文件。第一类为LLVM Bitcode文件（.bc），由wllvm工具链在编译过程中提取，包含完整的程序中间表示。第二类为代码属性图数据库（cpg.bin），由Joern工具解析源码生成，融合了控制流图与数据依赖图。第三类为调试信息映射表，从Bitcode中解析DWARF调试信息，建立函数名到源文件路径和行号的映射关系。

静态分析阶段输出候选函数列表，每个候选函数包含以下字段：name（函数名）、args（参数列表，包含类型和名称）、return_type（返回类型）、pattern（漏洞模式）、detector（检测器来源）、imbalance（开销失衡值，仅Flush+Reload）、code_snippet（触发指令，仅Spectre系列）、detector_hints（检测提示，包含attack_types约束）。

AI分析阶段输出审计报告列表，每个报告项包含以下字段：func_name（函数名）、attack_type（攻击类型，严格受白名单约束）、reason（漏洞原因分析）、leaked_info（泄露信息类型）。

### 输出接口定义

模块通过Server-Sent Events（SSE）协议向前端推送实时分析数据。事件类型分为四类：系统控制台事件（start、info、warning、error、phase_0至phase_4）用于驱动终端模拟器展示；报表图表事件（candidates、shortlist、audit_reports）用于渲染数据表格和可视化图表；AI思维活动事件（agent_step、tool_call、tool_result）用于展示AI的推理过程；完结信号事件（final_report、finished）用于标记分析完成。

最终报告以JSON格式输出，包含项目概述、检测统计、漏洞详情、修复建议等章节。报告同时支持Markdown格式导出，便于集成到文档系统中。

## 特征工程方法

### LLVM IR特征提取

静态分析引擎从LLVM IR中提取多层次特征。指令级特征包括操作码类型、操作数数量、寄存器使用模式等。基本块级特征包括指令数量、调用密度、计算复杂度等。函数级特征包括基本块数量、控制流复杂度、参数类型分布等。数据流特征包括定义-使用链、活跃变量分析、别名分析结果等。

**控制流特征**：引擎构建函数的控制流图（CFG），计算圈复杂度、分支深度、循环嵌套层数等拓扑特征。对于条件分支，额外计算两侧路径的指令数量差异、调用指令数量差异、内存访问次数差异等执行时间相关特征。

**数据流特征**：引擎通过数据依赖分析识别秘密数据的影响范围。对于每个函数参数，追踪其在函数体内的传播路径，识别是否影响控制流决策、数组索引计算、内存地址计算等敏感操作。同时分析指针参数的解引用模式，识别潜在的越界访问风险。

### 代码属性图特征

Joern引擎生成的代码属性图（CPG）融合了抽象语法树（AST）、控制流图（CFG）和数据依赖图（PDG）三种表示。AI Agent通过CPG查询获取以下特征：函数调用关系（识别跨函数的数据流传播）、类型定义信息（识别结构体成员的敏感属性）、宏展开结果（识别编译时常量的实际值）、注释信息（识别开发者标注的安全相关说明）。

**语义特征提取**：AI引擎结合项目README文档和代码注释，提取业务语义特征。通过自然语言处理技术识别关键词（如key、secret、password、token、encrypt、decrypt等），判断函数是否处理敏感数据。该特征用于在第三阶段过滤不涉及核心资产的浅层风险函数。

### 时序特征建模

针对侧信道攻击的时间维度特性，引擎构建了时序特征模型。对于Flush+Reload模式，计算分支两侧基本块的执行时间差值，该差值越大，泄露风险越高。对于Spectre V4模式，计算Store指令与后续Load指令之间的指令距离，该距离越小，陈旧数据被推测执行利用的可能性越高。对于Prime+Probe模式，计算数组访问的跨度特征，跨度越大，缓存竞争泄露的信号越强。

## 模型选型与架构设计

### 大语言模型选型

AI推理引擎选用DeepSeek大模型作为核心推理基座，主要基于以下考量。首先，DeepSeek在代码理解和逻辑推理任务上表现优异，能够准确分析复杂的C/C++系统代码。其次，DeepSeek支持Function Calling机制，允许模型在推理过程中主动调用外部工具获取补充信息。第三，DeepSeek提供OpenAI兼容接口，便于集成到现有系统中，同时支持私有化部署以满足数据安全要求。

模型配置参数包括：model（模型名称，默认deepseek-chat）、temperature（温度参数，默认0.7）、max_tokens（最大输出长度，默认4096）、top_p（核采样参数，默认0.9）。针对安全审计场景，建议将temperature设置为较低值（0.3-0.5），以提高输出的确定性和一致性。

### Agent架构设计

AI引擎采用ReAct（Reasoning and Acting）架构实现工具调用能力。Agent在每轮推理中首先进行思考（Reasoning），判断是否需要调用工具获取额外信息；如果需要，则生成工具调用请求（Acting）；系统执行工具并返回结果；Agent根据结果继续推理，直到生成最终结论。

工具定义采用OpenAI Function Calling规范，包含name（工具名称）、description（工具描述）、parameters（参数schema）三个字段。当前实现的工具为lookup_symbol，用于查询结构体定义、宏定义、类型声明等符号信息。工具执行器（JoernAgent）维护与Joern引擎的长连接，通过pexpect库实现高效的交互式查询。

### 多阶段推理流程

AI推理分为两个阶段。第一阶段为筛选阶段（phase1_screening），输入项目README文档和候选函数列表，输出涉及敏感数据的核心函数子集。该阶段使用轻量级提示词，快速过滤误报。第二阶段为深度审计阶段（phase2_batch_deep_audit），输入核心函数的完整源码和静态分析结果，输出详细的漏洞报告。该阶段使用复杂提示词，引导AI进行深入的代码分析和漏洞推理。

提示词工程遵循以下原则：明确角色定位（安全审计专家）、提供充足上下文（项目背景、函数源码、静态分析结果）、约束输出格式（JSON数组）、引导工具使用（遇到未知符号主动查询）、强制类型校验（只能在白名单内选择攻击类型）。

## 训练与推理流程

### 推理流程设计

推理流程采用流式处理架构，支持大规模项目的增量分析。流程启动时，首先初始化JoernAgent并加载CPG数据库，建立与Joern引擎的长连接。然后依次执行静态检测器，收集候选函数列表。接着调用AI进行筛选，过滤不涉及敏感数据的函数。最后对剩余函数进行批量深度审计，生成最终报告。

**批处理策略**：为避免单次请求过大导致超时或质量下降，引擎采用分批处理策略。每批处理5-10个函数，批次之间插入短暂延迟以避免API限流。对于大型项目，支持断点续传机制，已分析的函数结果持久化存储，中断后可从断点恢复。

**错误处理机制**：推理流程实现了完善的错误处理机制。对于Joern查询超时，采用正则表达式回退方案从源码中直接提取定义。对于AI输出格式错误，采用JSON修复算法尝试解析。对于攻击类型校验失败，自动修正为"无"并记录原因。所有错误信息通过emit_callback回调函数推送到前端，便于用户了解分析进度。


## 性能优化策略

### 静态分析优化

静态分析引擎采用多种优化策略提升处理速度。指令遍历优化方面，对于大型函数，采用惰性求值策略，仅在需要时才计算基本块开销，避免不必要的全量计算。缓存机制方面，对已分析的函数结果进行缓存，避免重复分析。并行处理方面，多个检测器可并行执行，充分利用多核CPU资源。

**内存优化**：LLVM Bitcode文件可能非常大，引擎采用流式解析策略，避免一次性加载整个文件到内存。对于超大型项目，支持按编译单元分片分析，最后合并结果。

### AI推理优化

AI推理是整个流程的性能瓶颈，采用以下策略优化。连接复用方面，JoernAgent维护与Joern引擎的常驻连接，避免每次查询都重新启动JVM和加载CPG。批量查询方面，对于多个符号查询请求，合并为单次Joern调用，减少交互次数。缓存机制方面，对已查询的符号定义进行缓存，避免重复查询。

**提示词优化**：精简提示词长度，去除冗余描述，降低token消耗。对于相似函数，采用模板化提示词，复用公共部分。控制输出长度，限制reason字段的字数，避免过长的输出影响处理速度。

### 前端交互优化

SSE推送采用事件命名空间机制，前端可按需订阅特定频道，避免处理无关事件。对于大量日志输出（如编译过程），采用聚合推送策略，每秒推送一次摘要而非逐行推送。对于AI思维活动事件，采用增量更新策略，仅推送新增内容而非全量替换。

## 模块扩展性设计

### 检测器插件机制

检测模块采用插件化架构，新增检测器只需在core/detectors目录下创建Python模块，实现get_function_candidates(bc_file)接口即可。系统通过动态导入机制自动发现和加载检测器，无需修改核心代码。检测器可定义自己的漏洞模式名称、描述信息、阈值参数等，系统会自动将其纳入攻击类型白名单。

### 工具扩展机制

AI Agent的工具集采用可扩展设计，新增工具只需在tools列表中添加Function定义，并实现对应的执行函数即可。工具描述遵循OpenAI规范，模型会根据描述自动判断何时调用工具。未来可扩展的工具包括：符号执行引擎（用于路径可达性分析）、约束求解器（用于输入生成）、漏洞数据库查询（用于历史案例对比）等。

### 输出格式扩展

报告输出支持多种格式扩展。当前支持JSON和Markdown格式，未来可扩展支持HTML（带语法高亮）、PDF（用于正式报告）、SARIF（用于与IDE集成）等格式。输出模板采用Jinja2引擎，用户可自定义模板样式。

## 总结

AI侧信道漏洞检测模块通过混合分析架构实现了从源码到漏洞报告的端到端自动化。静态分析引擎基于LLVM IR实现了精确的指令级特征提取和污点传播分析，AI推理引擎基于大语言模型实现了语义理解和深度推理能力。模块采用插件化设计，支持检测器和工具的灵活扩展，为侧信道漏洞的自动化发现提供了完整的技术解决方案。

---

*文档版本：v1.0*  
*最后更新：2026年4月*
